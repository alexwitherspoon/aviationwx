name: Quality Assurance Tests

on:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "main" ]
  workflow_dispatch: {}

# Prevent concurrent runs - cancel old runs when new commit pushed
concurrency:
  group: qa-tests-${{ github.ref }}-${{ github.sha }}
  cancel-in-progress: true

permissions:
  contents: read
  actions: read

jobs:
  test:
    name: PHPUnit Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Validate commit and branch context
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "Workflow Context Validation"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "Commit SHA: ${{ github.sha }}"
          echo "Branch: ${{ github.ref_name }}"
          echo "Event: ${{ github.event_name }}"
          echo "Workflow Run ID: ${{ github.run_id }}"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          
          CHECKED_OUT_SHA=$(git rev-parse HEAD)
          if [ "$CHECKED_OUT_SHA" != "${{ github.sha }}" ]; then
            echo "❌ CRITICAL: Commit SHA mismatch!"
            echo "  Expected: ${{ github.sha }}"
            echo "  Checked out: $CHECKED_OUT_SHA"
            exit 1
          fi
          echo "✓ Commit SHA validated: $CHECKED_OUT_SHA"
      
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: apcu, gd, zip, curl, exif
          coverage: pcov
      
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libimage-exiftool-perl ffmpeg
      
      - name: Cache Composer dependencies
        uses: actions/cache@v4
        with:
          path: |
            vendor
            ~/.composer/cache
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-
      
      - name: Install Composer dependencies
        run: |
          composer install --no-interaction --prefer-dist --optimize-autoloader --no-progress
      
      - name: Validate PHP syntax
        run: |
          echo "Checking PHP syntax..."
          errors=$(find . -name "*.php" -not -path "./vendor/*" -exec php -l {} \; 2>&1 | grep -v "No syntax errors" || true)
          if [ ! -z "$errors" ]; then
            echo "❌ PHP syntax errors found:"
            echo "$errors"
            exit 1
          else
            echo "✓ All PHP files have valid syntax"
          fi
      
      - name: Run PHPUnit - Unit Tests (Critical)
        run: |
          vendor/bin/phpunit --testsuite Unit --testdox --log-junit unit-results.xml --coverage-clover unit-coverage.xml || exit_code=$?
          exit_code=${exit_code:-$?}
          if [ $exit_code -gt 1 ]; then
            echo "❌ Unit tests failed - check output above"
            exit 1
          elif [ $exit_code -eq 1 ]; then
            echo "⚠️  Unit tests passed with warnings"
          else
            echo "✓ Unit tests passed"
          fi
      
      - name: Run PHPUnit - Integration Tests
        run: |
          vendor/bin/phpunit --testsuite Integration --testdox --log-junit integration-results.xml --coverage-clover integration-coverage.xml || exit_code=$?
          exit_code=${exit_code:-$?}
          if [ $exit_code -gt 1 ]; then
            echo "❌ Integration tests failed - check output above"
            exit 1
          elif [ $exit_code -eq 1 ]; then
            echo "⚠️  Integration tests passed with warnings"
          else
            echo "✓ Integration tests passed"
          fi
      
      - name: Run Critical Safety Tests
        run: |
          vendor/bin/phpunit tests/Unit/WeatherCalculationsTest.php --testdox --stop-on-failure --no-coverage || exit_code1=$?
          exit_code1=${exit_code1:-$?}
          if [ $exit_code1 -gt 1 ]; then
            echo "❌ WeatherCalculationsTest failed"
            exit 1
          fi
          
          vendor/bin/phpunit tests/Unit/ErrorHandlingTest.php --testdox --stop-on-failure --no-coverage || exit_code2=$?
          exit_code2=${exit_code2:-$?}
          if [ $exit_code2 -gt 1 ]; then
            echo "❌ ErrorHandlingTest failed"
            exit 1
          fi
          
          vendor/bin/phpunit tests/Unit/WeatherAggregatorTest.php --testdox --stop-on-failure --no-coverage || exit_code3=$?
          exit_code3=${exit_code3:-$?}
          if [ $exit_code3 -gt 1 ]; then
            echo "❌ WeatherAggregatorTest failed"
            exit 1
          fi
          
          echo "✓ Critical safety tests passed (warnings allowed)"
      
      - name: Run Webcam Refresh Initialization Tests
        run: |
          echo "Running webcam refresh initialization tests..."
          echo "Testing CAM_TS initialization and data attributes..."
          vendor/bin/phpunit tests/Integration/WebcamRefreshInitializationTest.php --testdox --stop-on-failure || exit_code=$?
          exit_code=${exit_code:-$?}
          if [ $exit_code -gt 1 ]; then
            echo "❌ WebcamRefreshInitializationTest failed"
            exit 1
          elif [ $exit_code -eq 1 ]; then
            echo "⚠️  WebcamRefreshInitializationTest passed with warnings (expected in CI without coverage driver)"
          else
            echo "✓ WebcamRefreshInitializationTest passed"
          fi
      
      - name: Run Weather Aggregation Tests
        run: |
          vendor/bin/phpunit tests/Unit/WeatherAggregatorTest.php --testdox --stop-on-failure || exit_code=$?
          exit_code=${exit_code:-$?}
          if [ $exit_code -gt 1 ]; then
            echo "❌ WeatherAggregatorTest failed"
            exit 1
          elif [ $exit_code -eq 1 ]; then
            echo "⚠️  WeatherAggregatorTest passed with warnings (expected in CI without coverage driver)"
          else
            echo "✓ WeatherAggregatorTest passed"
          fi
          
          # Verify VRB wind direction handling
          vendor/bin/phpunit tests/Unit/ErrorHandlingTest.php --filter testParseMETARResponse_VariableWindDirection --testdox || exit_code=$?
          exit_code=${exit_code:-$?}
          if [ $exit_code -gt 1 ]; then
            echo "❌ VRB wind direction handling test failed"
            exit 1
          fi
          
          vendor/bin/phpunit tests/Unit/ErrorHandlingTest.php --filter testParseMETARResponse_NonNumericWindSpeed --testdox || exit_code=$?
          exit_code=${exit_code:-$?}
          if [ $exit_code -gt 1 ]; then
            echo "❌ Non-numeric wind speed handling test failed"
            exit 1
          fi
          
          echo "✓ Weather aggregation and error handling tests passed"
      
      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: critical-test-results
          path: |
            unit-results.xml
            integration-results.xml
            unit-coverage.xml
            integration-coverage.xml
          if-no-files-found: ignore
          retention-days: 7

  lint:
    name: Code Validation & Linting
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Validate commit and branch context
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "Workflow Context Validation"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "Commit SHA: ${{ github.sha }}"
          echo "Branch: ${{ github.ref_name }}"
          echo "Event: ${{ github.event_name }}"
          echo "Workflow Run ID: ${{ github.run_id }}"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          
          CHECKED_OUT_SHA=$(git rev-parse HEAD)
          if [ "$CHECKED_OUT_SHA" != "${{ github.sha }}" ]; then
            echo "❌ CRITICAL: Commit SHA mismatch!"
            exit 1
          fi
          echo "✓ Commit SHA validated: $CHECKED_OUT_SHA"
      
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: apcu, gd, zip, curl, exif
          coverage: none
      
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libimage-exiftool-perl ffmpeg
      
      - name: Cache Composer dependencies
        uses: actions/cache@v4
        with:
          path: |
            vendor
            ~/.composer/cache
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-
      
      - name: Install Composer dependencies
        run: |
          composer install --no-interaction --prefer-dist --optimize-autoloader --no-progress
      
      - name: Validate Weather Pipeline
        run: |
          echo "🔍 Validating unified weather pipeline..."
          
          # Check that UnifiedFetcher exists
          if ! grep -q "function fetchWeatherUnified" lib/weather/UnifiedFetcher.php; then
            echo "❌ CRITICAL: fetchWeatherUnified function missing!"
            exit 1
          fi
          
          # Check that WeatherAggregator exists
          if ! grep -q "class WeatherAggregator" lib/weather/WeatherAggregator.php; then
            echo "❌ CRITICAL: WeatherAggregator class missing!"
            exit 1
          fi
          
          if ! grep -q "wdir.*VRB\|'VRB'" lib/weather/adapter/metar-v1.php; then
            echo "⚠️  Warning: VRB wind direction handling may be missing"
          fi
          
          if ! grep -q "is_numeric.*wdir\|is_numeric.*wspd" lib/weather/adapter/metar-v1.php; then
            echo "⚠️  Warning: Numeric validation for wind data may be missing"
          fi
          
          echo "✓ Weather pipeline validation complete"
      
      - name: JavaScript Static Analysis
        run: |
          echo "🔍 Checking for PHP functions in JavaScript..."
          php scripts/validate-javascript.php
      
      - name: ESLint JavaScript Linting
        run: |
          echo "🔍 Linting JavaScript code with ESLint..."
          if [ -f package.json ]; then
            npm install
            npm run lint:js || {
              echo "❌ ESLint found JavaScript errors. Please fix them."
              echo ""
              echo "Note: Some parsing errors may be false positives due to PHP-embedded JavaScript."
              echo "See docs/ESLINT_KNOWN_LIMITATIONS.md for details on known limitations."
              echo ""
              echo "Review the errors above and fix any real issues (scope, undefined variables, etc.)."
              echo "Parsing errors in PHP-embedded JavaScript are documented false positives."
              exit 1
            }
          else
            echo "⚠️  package.json not found, skipping ESLint"
          fi
      
      - name: API Endpoint Validation
        run: |
          echo "🔍 Validating API endpoint URLs in JavaScript..."
          
          # Check that weather.php calls use /api/weather.php
          if grep -r "weather\.php" pages/*.php | grep -v "/api/weather\.php" | grep -v "api/weather\.php" | grep -v "//.*weather\.php" | grep -v "\*.*weather\.php"; then
            echo "❌ Found weather.php calls that don't use /api/weather.php"
            echo "All weather API calls should use /api/weather.php"
            exit 1
          fi
          
          echo "✓ All weather API calls use correct endpoint"
      
      - name: Check for required files
        run: |
          required_files=(
            "index.php"
            "api/weather.php"
            "api/webcam.php"
            "lib/config.php"
            "lib/rate-limit.php"
            "lib/constants.php"
            "lib/circuit-breaker.php"
            "scripts/process-push-webcams.php"
            "scripts/sync-push-config.php"
            "lib/push-webcam-validator.php"
            "pages/config-generator.php"
            "pages/status.php"
          )
          for file in "${required_files[@]}"; do
            if [ ! -f "$file" ]; then
              echo "❌ Required file missing: $file"
              exit 1
            fi
          done
          echo "✓ All required files present"
      
      - name: Validate JSON files
        run: |
          if [ -f "config/airports.json.example" ]; then
            php -r "json_decode(file_get_contents('config/airports.json.example'), true); if (json_last_error() !== JSON_ERROR_NONE) { echo 'Invalid JSON: ' . json_last_error_msg() . PHP_EOL; exit(1); }"
            echo "✓ config/airports.json.example is valid JSON"
          fi
      
      - name: Test config utilities
        run: |
          php -r "
          require_once 'lib/config.php';
          
          // Test validation
          \$tests = [
            ['kspb', true],
            ['KSPB', true], // Should be lowercased
            ['kx12', true],
            ['invalid!!', false],
            ['', false],
            ['toolong123', false],
            ['ab', false], // Too short
          ];
          
          foreach (\$tests as [\$id, \$expected]) {
            \$result = validateAirportId(\$id);
            if (\$result !== \$expected) {
              echo 'FAIL: validateAirportId(\"' . \$id . '\") returned ' . (\$result ? 'true' : 'false') . ', expected ' . (\$expected ? 'true' : 'false') . PHP_EOL;
              exit(1);
            }
          }
          
          echo '✓ Airport ID validation tests passed' . PHP_EOL;
          "
      
      - name: Test push webcam configuration validation
        run: |
          php -r "
          require_once 'lib/push-webcam-validator.php';
          
          // Test push webcam config validation
          \$validConfig = [
            'name' => 'Test Camera',
            'push_config' => [
              'username' => 'aB3xK9mP2qR7vN',
              'password' => 'mK8pL3nQ6rT9vW',
              'protocol' => 'sftp',
              'port' => 2222,
              'max_file_size_mb' => 10,
              'allowed_extensions' => ['jpg', 'jpeg']
            ],
            'refresh_seconds' => 300
          ];
          
          \$result = validatePushWebcamConfig(\$validConfig, 'test', 0);
          
          if (!\$result['valid']) {
            echo 'FAIL: Valid push webcam config was rejected' . PHP_EOL;
            echo 'Errors: ' . implode(', ', \$result['errors']) . PHP_EOL;
            exit(1);
          }
          
          echo '✓ Push webcam configuration validation working' . PHP_EOL;
          "
      
      - name: Check for security issues
        run: |
          dangerous_patterns=(
            "eval("
            "base64_decode.*\$_GET"
            "base64_decode.*\$_POST"
            "file_get_contents.*\$_GET"
            "file_get_contents.*\$_POST"
          )
          
          for pattern in "${dangerous_patterns[@]}"; do
            matches=$(grep -r "$pattern" --include="*.php" . | grep -v "test" | grep -v "diagnostics" || true)
            if [ ! -z "$matches" ]; then
              echo "⚠️  Potentially dangerous pattern found: $pattern"
              echo "$matches"
            fi
          done
          
          echo "✓ Security check complete"
      
      - name: Test rate limiting functions
        run: |
          php -r "
          require_once 'lib/rate-limit.php';
          
          if (!function_exists('checkRateLimit')) {
            echo 'FAIL: checkRateLimit function not found' . PHP_EOL;
            exit(1);
          }
          
          if (!function_exists('getRateLimitRemaining')) {
            echo 'FAIL: getRateLimitRemaining function not found' . PHP_EOL;
            exit(1);
          }
          
          if (!function_exists('checkRateLimitFileBased')) {
            echo 'FAIL: checkRateLimitFileBased function not found (file-based fallback)' . PHP_EOL;
            exit(1);
          }
          
          echo '✓ Rate limiting functions exist' . PHP_EOL;
          "
      
      - name: Test circuit breaker functions
        run: |
          php -r "
          require_once 'lib/circuit-breaker.php';
          
          if (!function_exists('checkWeatherCircuitBreaker')) {
            echo 'FAIL: checkWeatherCircuitBreaker function not found' . PHP_EOL;
            exit(1);
          }
          
          if (!function_exists('recordWeatherFailure')) {
            echo 'FAIL: recordWeatherFailure function not found' . PHP_EOL;
            exit(1);
          }
          
          if (!function_exists('recordWeatherSuccess')) {
            echo 'FAIL: recordWeatherSuccess function not found' . PHP_EOL;
            exit(1);
          }
          
          if (!function_exists('checkWebcamCircuitBreaker')) {
            echo 'FAIL: checkWebcamCircuitBreaker function not found' . PHP_EOL;
            exit(1);
          }
          
          if (!function_exists('recordWebcamFailure')) {
            echo 'FAIL: recordWebcamFailure function not found' . PHP_EOL;
            exit(1);
          }
          
          if (!function_exists('recordWebcamSuccess')) {
            echo 'FAIL: recordWebcamSuccess function not found' . PHP_EOL;
            exit(1);
          }
          
          echo '✓ Circuit breaker functions exist' . PHP_EOL;
          "
      
      - name: Test constants file
        run: |
          php -r "
          require_once 'lib/constants.php';
          
          \$required_constants = [
            'DEFAULT_WEBCAM_REFRESH',
            'DEFAULT_WEATHER_REFRESH',
            'RATE_LIMIT_WEATHER_MAX',
            'RATE_LIMIT_WEATHER_WINDOW',
            'BACKOFF_BASE_SECONDS',
            'CACHE_FILE_MAX_SIZE',
            'CURL_TIMEOUT',
            'CONFIG_CACHE_TTL',
            'DEFAULT_STALE_FAILCLOSED_SECONDS'
          ];
          
          foreach (\$required_constants as \$const) {
            if (!defined(\$const)) {
              echo 'FAIL: Constant ' . \$const . ' not defined' . PHP_EOL;
              exit(1);
            }
          }
          
          echo '✓ Required constants defined' . PHP_EOL;
          "
      
      - name: Validate push webcam functions
        run: |
          php -r "
          require_once 'lib/push-webcam-validator.php';
          require_once 'scripts/process-push-webcams.php';
          require_once 'scripts/sync-push-config.php';
          
          \$functions = [
            'validatePushWebcamConfig',
            'processPushWebcams',
            'syncPushConfig',
            'getCameraRefreshSeconds',
            'findNewestValidImage',
            'validateImageFile'
          ];
          
          foreach (\$functions as \$func) {
            if (!function_exists(\$func)) {
              echo 'FAIL: Function ' . \$func . ' not found' . PHP_EOL;
              exit(1);
            }
          }
          
          echo '✓ Push webcam functions exist' . PHP_EOL;
          "
      
      - name: Check Dockerfile
        run: |
          if [ ! -f "docker/Dockerfile" ]; then
            echo "❌ Dockerfile missing"
            exit 1
          fi
          
          grep -q "FROM php:8.3-apache" docker/Dockerfile || (echo "❌ Dockerfile doesn't use PHP 8.3-apache" && exit 1)
          grep -q "ffmpeg" docker/Dockerfile || (echo "⚠️  ffmpeg not found in Dockerfile" || true)
          grep -q "vsftpd" docker/Dockerfile || (echo "❌ vsftpd not found in Dockerfile (required for push webcams)" && exit 1)
          grep -q "openssh-server" docker/Dockerfile || (echo "❌ openssh-server not found in Dockerfile (required for SFTP)" && exit 1)
          grep -q "certbot" docker/Dockerfile || (echo "⚠️  certbot not found in Dockerfile (required for FTPS)" || true)
          grep -q "logrotate" docker/Dockerfile || (echo "⚠️  logrotate not found in Dockerfile (recommended for FTP/SFTP logs)" || true)
          
          echo "✓ Dockerfile checks passed"
      
      - name: Check docker-compose files
        run: |
          if [ ! -f "docker/docker-compose.yml" ] || [ ! -f "docker/docker-compose.prod.yml" ]; then
            echo "❌ Docker Compose files missing"
            exit 1
          fi
          echo "✓ Docker Compose files present"

  build-docker-image:
    name: Build Docker Image
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Validate test fixture exists
        run: |
          if [ ! -f "tests/Fixtures/airports.json.test" ]; then
            echo "❌ Test fixture file missing: tests/Fixtures/airports.json.test"
            exit 1
          fi
          # Validate JSON using Python (available on ubuntu-latest)
          python3 -m json.tool tests/Fixtures/airports.json.test > /dev/null || {
            echo "❌ Test fixture is not valid JSON"
            exit 1
          }
          echo "✓ Test fixture is valid JSON"
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Build Docker image with cache
        uses: docker/build-push-action@v5
        id: build
        with:
          context: .
          file: ./docker/Dockerfile
          push: false
          tags: aviationwx:test
          cache-from: type=gha
          cache-to: type=gha,mode=max
          outputs: type=docker,dest=/tmp/aviationwx-test.tar
      
      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v4
        with:
          name: docker-image-test
          path: /tmp/aviationwx-test.tar
          retention-days: 1
          compression-level: 6

  performance-tests:
    name: Performance Tests
    runs-on: ubuntu-latest
    continue-on-error: true
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: apcu, gd, zip, curl, json, mbstring
          coverage: none
      
      - name: Cache Composer dependencies
        uses: actions/cache@v4
        with:
          path: |
            vendor
            ~/.composer/cache
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-
      
      - name: Install Composer dependencies
        run: |
          composer install --no-interaction --prefer-dist --optimize-autoloader --no-progress
      
      - name: Run Performance Tests
        run: |
          vendor/bin/phpunit --testsuite Performance --testdox --log-junit performance-results.xml --no-coverage || [ $? -le 1 ]
        continue-on-error: true
        env:
          CONFIG_PATH: tests/Fixtures/airports.json.test
  
  e2e-tests:
    name: End-to-End Tests
    runs-on: ubuntu-latest
    needs: build-docker-image
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Validate commit and branch context
        run: |
          echo "Commit SHA: ${{ github.sha }}"
          echo "Branch: ${{ github.ref_name }}"
          CHECKED_OUT_SHA=$(git rev-parse HEAD)
          if [ "$CHECKED_OUT_SHA" != "${{ github.sha }}" ]; then
            echo "❌ CRITICAL: Commit SHA mismatch!"
            exit 1
          fi
          echo "✓ Commit SHA validated: $CHECKED_OUT_SHA"
      
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: apcu, gd, zip, curl, json, mbstring
          coverage: none
      
      - name: Cache Composer dependencies
        uses: actions/cache@v4
        with:
          path: |
            vendor
            ~/.composer/cache
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-
      
      - name: Install Composer dependencies
        run: |
          composer install --no-interaction --prefer-dist --optimize-autoloader --no-progress
      
      - name: Setup test configuration
        run: |
          # Note: airports.json is NOT in repository - only exists on production host
          # CI uses test fixtures (tests/Fixtures/airports.json.test) via CONFIG_PATH
          # For Docker container, we need a valid config file mounted
          # Use test fixture instead of example (example has invalid keys like _example_kpdx)
          if [ ! -f config/airports.json ]; then
            cp tests/Fixtures/airports.json.test config/airports.json
            echo "✓ Created config/airports.json from test fixture for Docker setup"
          else
            # Verify existing config is valid (not from example with invalid keys)
            if grep -q "_example" config/airports.json; then
              echo "⚠️  Existing config has invalid example keys, replacing with test fixture"
              cp tests/Fixtures/airports.json.test config/airports.json
            fi
          fi
          # Verify config file exists and is valid JSON
          if [ ! -f config/airports.json ]; then
            echo "❌ Failed to create config/airports.json"
            exit 1
          fi
          php -r "json_decode(file_get_contents('config/airports.json'), true); if (json_last_error() !== JSON_ERROR_NONE) { echo 'Invalid JSON: ' . json_last_error_msg() . PHP_EOL; exit(1); }"
          echo "✓ Config file is valid JSON"
      
      - name: Download Docker image artifact
        uses: actions/download-artifact@v4
        with:
          name: docker-image-test
          path: /tmp
      
      - name: Load Docker image
        run: |
          docker load -i /tmp/aviationwx-test.tar
          docker tag aviationwx:test aviationwx:latest || true
          echo "✓ Docker image loaded and tagged"
      
      - name: Start Docker containers
        env:
          CONFIG_PATH: ${{ github.workspace }}/config/airports.json
          APP_ENV: testing
        run: |
          docker compose -f docker/docker-compose.yml up -d
          docker compose -f docker/docker-compose.yml ps
      
      - name: Wait for health check
        run: |
          bash scripts/wait-for-service-health.sh 60 http://localhost:8080
      
      - name: Run E2E Tests
        run: |
          vendor/bin/phpunit --testsuite E2E --testdox --log-junit e2e-results.xml --no-coverage
          exit_code=$?
          if [ $exit_code -eq 0 ]; then
            echo "✓ All E2E tests passed"
          elif [ $exit_code -eq 1 ]; then
            echo "⚠️  E2E tests passed with warnings (this is OK)"
            exit 0
          else
            echo "❌ E2E tests failed"
            exit $exit_code
          fi
        env:
          TEST_API_URL: http://localhost:8080
      
      - name: Cleanup
        if: always()
        run: |
          docker compose -f docker/docker-compose.yml down
      
      - name: Upload E2E Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-test-results
          path: e2e-results.xml
          if-no-files-found: ignore
          retention-days: 7
  
  smoke-tests:
    name: Smoke Tests - Local
    runs-on: ubuntu-latest
    needs: build-docker-image
    if: github.event_name == 'pull_request' || github.event_name == 'push'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Validate commit and branch context
        run: |
          echo "Commit SHA: ${{ github.sha }}"
          echo "Branch: ${{ github.ref_name }}"
          CHECKED_OUT_SHA=$(git rev-parse HEAD)
          if [ "$CHECKED_OUT_SHA" != "${{ github.sha }}" ]; then
            echo "❌ CRITICAL: Commit SHA mismatch!"
            exit 1
          fi
          echo "✓ Commit SHA validated: $CHECKED_OUT_SHA"
      
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: apcu, gd, zip, curl, json, mbstring
          coverage: none
      
      - name: Cache Composer dependencies
        uses: actions/cache@v4
        with:
          path: |
            vendor
            ~/.composer/cache
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-
      
      - name: Install Composer dependencies
        run: |
          composer install --no-interaction --prefer-dist --optimize-autoloader --no-progress
      
      - name: Setup test configuration
        run: |
          # Note: airports.json is NOT in repository - only exists on production host
          # CI uses test fixtures (tests/Fixtures/airports.json.test) via CONFIG_PATH
          # For Docker container, we need a valid config file mounted
          # Use test fixture instead of example (example has invalid keys like _example_kpdx)
          if [ ! -f config/airports.json ]; then
            cp tests/Fixtures/airports.json.test config/airports.json
            echo "✓ Created config/airports.json from test fixture for Docker setup"
          else
            # Verify existing config is valid (not from example with invalid keys)
            if grep -q "_example" config/airports.json; then
              echo "⚠️  Existing config has invalid example keys, replacing with test fixture"
              cp tests/Fixtures/airports.json.test config/airports.json
            fi
          fi
          # Verify config file exists and is valid JSON
          if [ ! -f config/airports.json ]; then
            echo "❌ Failed to create config/airports.json"
            exit 1
          fi
          php -r "json_decode(file_get_contents('config/airports.json'), true); if (json_last_error() !== JSON_ERROR_NONE) { echo 'Invalid JSON: ' . json_last_error_msg() . PHP_EOL; exit(1); }"
          echo "✓ Config file is valid JSON"
      
      - name: Download Docker image artifact
        uses: actions/download-artifact@v4
        with:
          name: docker-image-test
          path: /tmp
      
      - name: Load Docker image
        run: |
          docker load -i /tmp/aviationwx-test.tar
          docker tag aviationwx:test aviationwx:latest || true
          echo "✓ Docker image loaded and tagged"
      
      - name: Start Docker containers
        run: |
          docker compose -f docker/docker-compose.yml up -d
      
      - name: Wait for health check
        run: |
          bash scripts/wait-for-service-health.sh 60 http://localhost:8080
      
      - name: Run Smoke Tests (Local)
        run: |
          vendor/bin/phpunit --testsuite Smoke --testdox --log-junit smoke-results.xml --no-coverage
          exit_code=$?
          if [ $exit_code -eq 0 ]; then
            echo "✓ All smoke tests passed"
          elif [ $exit_code -eq 1 ]; then
            echo "⚠️  Smoke tests passed with warnings (this is OK)"
            exit 0
          else
            echo "❌ Smoke tests failed"
            exit $exit_code
          fi
        env:
          TEST_API_URL: http://localhost:8080
      
      - name: Cleanup
        if: always()
        run: |
          docker compose -f docker/docker-compose.yml down
      
      - name: Upload Smoke Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-test-results
          path: smoke-results.xml
          if-no-files-found: ignore
          retention-days: 7
  
  external-links-tests:
    name: External Links Validation
    runs-on: ubuntu-latest
    continue-on-error: true
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: apcu, gd, zip, curl, json, mbstring
          coverage: none
      
      - name: Cache Composer dependencies
        uses: actions/cache@v4
        with:
          path: |
            vendor
            ~/.composer/cache
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-
      
      - name: Install Composer dependencies
        run: |
          composer install --no-interaction --prefer-dist --optimize-autoloader --no-progress
      
      - name: Run External Links Validation Tests
        run: |
          vendor/bin/phpunit tests/Integration/ExternalLinksTest.php --testdox --log-junit external-links-results.xml --no-coverage || [ $? -le 1 ]
        continue-on-error: true
        env:
          CONFIG_PATH: tests/Fixtures/airports.json.test
  
  browser-tests:
    name: Browser Tests - ${{ matrix.test-file }}
    runs-on: ubuntu-latest
    needs: build-docker-image
    continue-on-error: true
    strategy:
      fail-fast: false
      matrix:
        test-file: 
          - 'aviationwx.spec.js'
          - 'cache-and-stale-data.spec.js'
          - 'performance-optimizations.spec.js'
          - 'api-endpoint-validation.spec.js'
          - 'javascript-validity.spec.js'
          - 'weather-data-visibility.spec.js'
          - 'e2e-weather-flow.spec.js'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: tests/Browser/package.json
      
      - name: Install Playwright dependencies
        working-directory: tests/Browser
        run: |
          npm install
          npx playwright install --with-deps chromium
      
      - name: Setup test configuration
        run: |
          # Note: airports.json is NOT in repository - only exists on production host
          # CI uses test fixtures (tests/Fixtures/airports.json.test) via CONFIG_PATH
          # For Docker container, we need a valid config file mounted
          # Use test fixture instead of example (example has invalid keys like _example_kpdx)
          if [ ! -f config/airports.json ]; then
            cp tests/Fixtures/airports.json.test config/airports.json
            echo "✓ Created config/airports.json from test fixture for Docker setup"
          else
            # Verify existing config is valid (not from example with invalid keys)
            if grep -q "_example" config/airports.json; then
              echo "⚠️  Existing config has invalid example keys, replacing with test fixture"
              cp tests/Fixtures/airports.json.test config/airports.json
            fi
          fi
          # Verify config file exists and is valid JSON
          if [ ! -f config/airports.json ]; then
            echo "❌ Failed to create config/airports.json"
            exit 1
          fi
          php -r "json_decode(file_get_contents('config/airports.json'), true); if (json_last_error() !== JSON_ERROR_NONE) { echo 'Invalid JSON: ' . json_last_error_msg() . PHP_EOL; exit(1); }"
          echo "✓ Config file is valid JSON"
      
      - name: Download Docker image artifact
        uses: actions/download-artifact@v4
        with:
          name: docker-image-test
          path: /tmp
      
      - name: Load Docker image
        run: |
          docker load -i /tmp/aviationwx-test.tar
          docker tag aviationwx:test aviationwx:latest || true
          echo "✓ Docker image loaded and tagged"
      
      - name: Start Docker containers
        run: |
          docker compose -f docker/docker-compose.yml up -d
          docker compose -f docker/docker-compose.yml ps
      
      - name: Wait for service to be ready
        run: |
          bash scripts/wait-for-service-health.sh 60 http://localhost:8080
      
      - name: Run Browser Tests
        working-directory: tests/Browser
        run: |
          npx playwright test tests/${{ matrix.test-file }} || [ $? -eq 1 ]
        continue-on-error: true
        env:
          TEST_BASE_URL: http://localhost:8080
          PLAYWRIGHT_WORKERS: 6
      
      - name: Upload Playwright Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-${{ matrix.test-file }}
          path: tests/Browser/playwright-report/
          retention-days: 7
      
      - name: Upload Screenshots (on failure only)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-screenshots-${{ matrix.test-file }}
          path: tests/Browser/test-results/**/*.png
          if-no-files-found: ignore
          retention-days: 7
      
      - name: Cleanup
        if: always()
        run: |
          docker compose -f docker/docker-compose.yml down
